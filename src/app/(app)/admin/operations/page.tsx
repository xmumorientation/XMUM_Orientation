// The former Committee operations surface now lives inside the unified Admin
// area. Its implementation is reused until the Admin module receives its
// dedicated section-by-section redesign.
export { default } from "@/app/(app)/committee/page";
